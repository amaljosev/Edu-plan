import 'package:flutter/material.dart';

class ScreenChatPrivate extends StatelessWidget {
  const ScreenChatPrivate({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Chat')),
    );
  } 
  
}
